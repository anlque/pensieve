{
  "name": "pensieve",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "build:extension": "vite build --mode extension && node scripts/prepare-extension.mjs",
    "check": "npm run build && node scripts/check-quality.mjs",
    "check:extension": "npm run build:extension && node scripts/check-quality.mjs --extension-package",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.8.0",
    "vite": "^7.0.0"
  }
}
